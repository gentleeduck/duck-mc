import type { NextConfig } from "next";

const config: NextConfig = {
  // Content build runs via the explicit `pnpm content` predev/prebuild
  // script so the example works without Velite's Next.js plugin.
};

export default config;
